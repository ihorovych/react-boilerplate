// import { takeLatest } from 'redux-saga/effects';

// import { handleSomeAction } from './someSaga';
// import { initSomeAction } from '../actions/index';

function* saga() {
  // console.log('SAGA_WORKING?');
  // yield takeLatest(initSomeAction, handleSomeAction);
}

export default saga;
